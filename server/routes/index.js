const express = require('express');
const router = express.Router();
const models = require('../models');
const doctorController = require('../controllers/doctor');

/* GET home page. */
//router.get('/:page', doctorController.getDoctors);


module.exports = router;
